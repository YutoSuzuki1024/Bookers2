<div class="sidebar"></div>
<main>
	<h2>Books</h2>
	<table>
		<tr>
			<th>""</th><th>Title</th><th>Opinion</th>
		</tr>
		<% @books.each do |book| %>
		<tr>
			<td><%= book.user_id %></td>
			<td><%= book.title %></td>
			<td><%= book.body %></td>
		</tr>
		<% end %>
	</table>
</main>